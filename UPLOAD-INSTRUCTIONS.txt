Upload these items to the ROOT of the GitHub repository:

1. styles.css — replace the existing root styles.css
2. assets/community-dallas-daytime.jpg — replace the existing file at the same path
3. .nojekyll — add this file to the repository root

Do not delete or replace index.html, schedule.html, script.js, or any other assets.

The .nojekyll file tells GitHub Pages to publish this static site directly without running Jekyll processing.
