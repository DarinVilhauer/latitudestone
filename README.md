# Latitude Stone Homepage v1.1

GitHub Pages-ready homepage preview.

Upload every file and the `assets` folder to the **root** of the GitHub repository. GitHub Pages should deploy from `main` / `(root)`.

This preview includes placeholder consultation email links and placeholder article dates/content that can be updated before public launch.
